```markdown
# 🎯 TypeScript Interview Questions - Blog Answers

## 1️⃣ What are some differences between interfaces and types in TypeScript?

TypeScript-এ object structure ডিফাইন করার জন্য সাধারণত দুইটি উপায় ব্যবহৃত হয়—**interface** এবং **type**। অনেক ক্ষেত্রে এরা একই কাজ করতে পারে, কিন্তু দুটির মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য আছে।

### ✅ 1. Interface “open”, কিন্তু Type “closed”

Interface পুনরায় ডিক্লেয়ার করলে সেটি merge হয়ে যায় 

```ts
interface User {
  name: string;
}

interface User {
  age: number;
}

const u: User = { name: "A", age: 20 }; // ✅ কোনো এরর নেই
```

অর্থাৎ, interface পরবর্তীতে বাড়ানো যায়।

কিন্তু type alias পুনরায় ডিক্লেয়ার করা যায় না:

```ts
type User = { name: string };
type User = { age: number }; // ❌ Error
```

### ✅ 2. Interfaces সাধারণত শুধু object structure বর্ণনা করতে ব্যবহৃত হয়।

Type দিয়ে আরও বেশি ধরনের বিষয় ডিফাইন করা যায়।  
Type alias শুধু object shape নয়, বরং:

- union  
- tuple  
- primitive  
- intersection  

ইত্যাদি ডিফাইন করতে পারে।

```ts
type Status = "success" | "error"; // Interface এ করা যায় না
```


## 3️⃣ Explain the difference between any, unknown, and never types in TypeScript.

# TypeScript এ `any`, `unknown`, এবং `never` টাইপের মধ্যে পার্থক্য

## 1. `any` টাইপ

`any` টাইপ হলো TypeScript এর সবচেয়ে flexible টাইপ। এটি যেকোনো ধরনের value গ্রহণ করতে পারে এবং TypeScript এর type checking সম্পূর্ণভাবে বন্ধ করে দেয়।

### বৈশিষ্ট্য:
- ✅ যেকোনো value assign করা যায়
- ✅ যেকোনো operation করা যায় বিনা error এ
- ⚠️ Type safety হারিয়ে যায়

### উদাহরণ:

```typescript
let value: any;
value = 5;           // ✓ ঠিক আছে
value = "hello";     // ✓ ঠিক আছে
value = true;        // ✓ ঠিক আছে
value.anyMethod();   // ✓ কোনো error দেখাবে না (runtime এ crash হতে পারে)
```

---

## 2. `unknown` টাইপ

`unknown` হলো `any` এর type-safe বিকল্প। এটিও যেকোনো value গ্রহণ করতে পারে, কিন্তু ব্যবহারের আগে type check করতে হয়।

### বৈশিষ্ট্য:
- ✅ যেকোনো value assign করা যায়
- ✅ ব্যবহারের আগে type checking বা type assertion প্রয়োজন
- ✅ Type safety বজায় থাকে

### উদাহরণ:

```typescript
let value: unknown;
value = 5;           // ✓ ঠিক আছে
value = "hello";     // ✓ ঠিক আছে

// value.toUpperCase();  // ✗ Error: type check ছাড়া use করা যাবে না

// সঠিক উপায়:
if (typeof value === "string") {
    value.toUpperCase();  // ✓ এখন ঠিক আছে
}
```

---

## 3. `never` টাইপ

`never` টাইপ এমন একটি value represent করে যা কখনোই ঘটবে না বা return হবে না। এটি সাধারণত এমন function এর জন্য ব্যবহার করা হয় যা কখনো শেষ হয় না বা সবসময় error throw করে।

### বৈশিষ্ট্য:
- ❌ কোনো value assign করা যায় না
- ✅ Function যা কখনো return করে না
- ✅ Unreachable code represent করে

### উদাহরণ:

```typescript
// Function যা সবসময় error throw করে
function throwError(message: string): never {
    throw new Error(message);
}

// Infinite loop
function infiniteLoop(): never {
    while (true) {
        // কখনো শেষ হবে না
    }
}

// Type narrowing এ
function processValue(value: string | number) {
    if (typeof value === "string") {
        // value হলো string
    } else if (typeof value === "number") {
        // value হলো number
    } else {
        // value হলো never (কখনো reach করবে না)
        const exhaustive: never = value;
    }
}
```
